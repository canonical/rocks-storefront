engine-strict=true
# @canonical/styles* pin their peer to typescript@^5.9.3; the project standardises
# on typescript@6. The mismatch is cosmetic (the DS types compile under TS 6), so
# relax strict peer resolution until the design system widens its range.
legacy-peer-deps=true
